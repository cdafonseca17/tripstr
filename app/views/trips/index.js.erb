function searchTrips(innerHTML) {
  const newTripForm = document.getElementById('card-trip-row');
  newTripForm.innerHTML = innerHTML;
}

searchTrips('<%= j render "trips/partials/cardtrip", trip: @trip %>');

var elements =  document.querySelectorAll('.card-trip');
elements.forEach(element => {
element.classList.add('animated', 'slideInUp');
});


//in JS
// Grab the comntainer element
// append to the container the list of items
