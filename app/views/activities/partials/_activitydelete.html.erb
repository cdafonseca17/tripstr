<%= link_to ('<i class="far fa-trash-alt"></i>').html_safe, trip_step_activity_path(@trip, step, activity),
      method: :delete,
      class: 'btn btn-link',
      data: { confirm: "Are you sure?" } %>
