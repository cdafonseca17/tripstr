<%= simple_form_for [trip, step, activity] do |f| %>
  <%= f.hidden_field :category, value: "Comment" %>
  <%= f.input :comment, placeholder: "Comment/Tips" %>
  <%= f.submit "save", class: "btn btn-primary" %>
<% end %>
